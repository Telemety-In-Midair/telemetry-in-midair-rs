[package]
name = "midair-proto"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Shared protocol for the telemetry-in-midair board: LoRa payloads, BLE extensions, radio TOML config, USB bulk framing"

# no_std (except under test). Depends only on gps-proto (also no_std) so both
# firmwares and host tests can use it.
[dependencies]
gps-proto = { path = "../../gps-proto" }
